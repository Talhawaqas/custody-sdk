# Required for the built-in /api/upload pinning route (get one at app.pinata.cloud)
PINATA_JWT=

# Optional -- defaults to BNB Chain Testnet's public RPC if unset
NEXT_PUBLIC_RPC_URL=https://data-seed-prebsc-1-s1.binance.org:8545
